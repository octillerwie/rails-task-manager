<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <%# form_for task do |f| %>
      <%# f.label "completed" %>
      <%# f.check_box :completed %>
    <%# end %>
    <li> <%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<%= link_to 'Add a new task', new_task_path %>
